# Phase 10: Real YOLO+EasyOCR Detector Configuration Template
# ============================================
# NOTE: These are now the DEFAULT values in config.py
# This file documents the expected configuration for Phase 10 spike branch.
# Override in .env only if you need different values.
# ============================================

# ============================================
# DETECTOR BACKEND CONFIGURATION
# ============================================
DETECTOR_BACKEND=yolo_ffmpeg

# ============================================
# YOLO MODEL CONFIGURATION
# ============================================
# Model from Hugging Face or local path
YOLO_MODEL=keremberke/yolov8n-license-plate

# Detection confidence threshold (0.0 - 1.0)
# Lower = more detections but more false positives
DETECT_CONFIDENCE=0.30

# Secondary threshold applied by DetectorAdapter
# Detections below this are filtered out
DETECTION_CONFIDENCE_THRESHOLD=0.30

# ============================================
# FRAME EXTRACTION CONFIGURATION
# ============================================
# Frames per second to extract from video
# Lower = faster processing, fewer detections
# Higher = slower processing, more detections
FRAME_EXTRACTION_FPS=1

# ============================================
# DEVICE CONFIGURATION
# ============================================
# Device for inference: "cpu" or "cuda"
# Auto-detected if not set, defaults to CPU in Replit
DEVICE=cpu

# ============================================
# BOUNDING BOX FILTERS
# ============================================
# Minimum bounding box dimensions (pixels)
# Boxes smaller than this are filtered out
MIN_BOX_WIDTH=20
MIN_BOX_HEIGHT=10

# ============================================
# INSTALLATION NOTES
# ============================================
# Required packages (already in requirements.txt):
# - ultralytics==8.0.196
# - easyocr==1.7.0
# - torch==2.1.0
# - torchvision==0.16.0
# - Pillow
#
# Required system dependency:
# - ffmpeg (pre-installed in Replit)
#
# ============================================
# ACTIVATION (Phase 10 is now default)
# ============================================
# 1. Install Python dependencies: pip install -r requirements.txt
# 2. Test with: python scripts/detector_smoketest.py
# 3. Start worker (yolo_ffmpeg is now the default backend)
#
# To revert to mock detector:
#   Add to .env: DETECTOR_BACKEND=mock
